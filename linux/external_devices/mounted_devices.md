# Примонтированные устройства

## Что это
История монтирования файловых систем. `/etc/fstab` — постоянные точки монтирования; `/proc/mounts` — текущее состояние.

## Пути
```
/etc/fstab                      ← постоянные точки монтирования
/proc/mounts                    ← текущие монтирования
/var/log/kern.log               ← события монтирования
```

## На какие вопросы отвечает
- Есть ли нестандартные NFS/CIFS-монтирования (потенциальная эксфильтрация или C2)?
- Монтируются ли внешние диски автоматически?
- Есть ли записи в fstab, добавленные злоумышленником?
- Есть ли примонтированные директории из /proс ([overmount](../security_state/rootkit_detection.md))?
- Есть ли нестандартные монтирования с типом tmpfs/ramfs?

## Парсинг
```bash
cat /proc/mounts
cat /etc/fstab
mount | grep -v "cgroup\|proc\|sysfs\|devtmpfs|tmpfs|ramfs"

# Нестандартные NFS/CIFS монтирования (потенциальная эксфильтрация или C2)
mount | grep -Ei "nfs|cifs|smb|sshfs"
```

## Инструменты
- UAC
- easy_triage